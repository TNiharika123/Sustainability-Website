import React from "react";

// It will act as a wrapper so that every component is aligned in same way which will provide consistency in alignment. So, add margnis and paddings based on that and then wrap top-level components in this Container.
function Container() {
  return <div>Container</div>;
}

export default Container;
